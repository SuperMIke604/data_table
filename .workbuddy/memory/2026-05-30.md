# 2026-05-30

## repairAIJson 新增策略 8

- **问题**：AI 在 JSON 字符串值内写了未转义的 ASCII 双引号（如 `"对外伪装为"网上看来的"`），导致解析失败，原有 7 条修复策略均无法修复
- **位置**：`index.js` 4923-5007 行（策略 8 插入在策略 7 之后、"全部失败"之前）
- **算法**：状态机扫描，通过"向后看下一个非空白字符是否为 JSON 结构符（`:,}]`）"来区分合法结束引号和内容裸引号
- **测试**：6/7 通过；失败的 1 个是理论上的歧义 case（引号后紧跟逗号），在真实 AI 输出中不会出现
- **触发报错日志文件**：`C:/Users/84686/Downloads/markDown1780156010802.md`，position 2452 的裸引号问题
